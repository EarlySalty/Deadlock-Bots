# Deadlock Bot – Python dependencies
# Generated from .venv (2026-02-18) — only direct project dependencies

# Discord
discord.py==2.7.1

# HTTP / Dashboard
aiohttp==3.14.1
aiohttp-jinja2==1.6
aiohttp-session==2.12.1
aiohttp-basicauth==1.2.0
aiodns==4.0.4  # async DNS resolver for aiohttp AsyncResolver (sonst Fallback-Warn-Flut)

# Database
aiosqlite==0.22.1
psycopg[binary]==3.3.4

# Templating
Jinja2==3.1.6

# Data / validation
pydantic==2.13.4
pydantic-settings==2.14.1
pytz==2026.2

# HTTP client
httpx==0.28.1
requests==2.34.2

# AI / LLM (Bild-Scam-Vision via OpenAI)
openai==2.43.0

# HTML parsing
beautifulsoup4==4.15.0

# Crypto / auth
cryptography==49.0.0
keyring==25.7.0
