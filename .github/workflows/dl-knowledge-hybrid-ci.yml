name: dl-knowledge hybrid CI

# PR coverage now belongs to Required PR Gate -> Discord config and workspace CI.
on:
  workflow_dispatch:
  push:
    branches: [feat/dl-knowledge-hybrid]

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/discord-config-ci.yml # zizmor: ignore[self-repository] -- Same-commit workflow; actionlint 1.7.12 does not parse dollar-slash syntax.
