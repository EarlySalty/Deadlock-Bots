name: Actual repository security and quality

# Bundled suites from the SHA-pinned CodeQL Action/CLI, not floating query packs.
# Language detection includes tracked Python, Rust, JavaScript and Actions.
queries:
  - uses: security-extended
  - uses: security-and-quality
paths-ignore:
  - .ci/**
  - rust/target/**
