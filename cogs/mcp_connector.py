# OBSOLET — MCP-Connector lebt jetzt im Rust-Bot (rust/bin/dl-bot/src/mcp.rs, Port 8890).
# Diese Datei ist absichtlich ein leerer Stub ohne `async def setup`, damit der
# Legacy-Python-Bot sie nicht als Cog lädt und kein Port-Konflikt entsteht.
# Kann gelöscht werden.
