[package]
name = "dl-brain-community"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

# Nur noch Bibliothek: das frühere Binary (Block D) ist ausgemustert, die
# Ledger-Typen und decide()/anonymize_for_privacy nutzt der Verbinder weiter.
[dependencies]
chrono.workspace = true
serde_json.workspace = true
sqlx.workspace = true

[lints]
workspace = true
