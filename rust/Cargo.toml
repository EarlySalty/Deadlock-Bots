# Rust-Rewrite der Deadlock-Bots (Strangler-Fig neben dem Python-Original).
# Architektur: 2 Binaries (dl-bot, dl-web) + Domain-Library-Crates.
# Plan und Verträge: siehe rust/docs/00-rewrite-plan.md
[workspace]
resolver = "2"
members = [
    "crates/dl-core",
    "crates/dl-webcore",
    "crates/dl-stats",
    "crates/dl-tierlist",
    "crates/dl-broker",
    "crates/dl-changelog",
    "crates/dl-discord",
    "crates/dl-bridges",
    "crates/dl-voice",
    "crates/dl-activity",
    "crates/dl-ai",
    "crates/dl-answer",
    "crates/dl-brain",
    "crates/dl-moderation",
    "crates/dl-squads",
    "crates/dl-community",
    "crates/dl-dashboard",
    "crates/dl-central-db",
    "crates/dl-central-etl",
    "crates/dl-server-as-code",
    "bin/dl-bot",
    "bin/dl-knowledge",
    "bin/dl-web",
    "bin/dl-twitch-invite-sync",
    "bin/dl-repostats",
    "bin/dl-insights-sync",
    "bin/dl-brain-community",
    "bin/dl-brain-feeder",
    "bin/dl-verbinder",
    "bin/dl-central-migrate",
    "bin/dl-central-sync",
    "bin/dl-infisical-env",
]

[workspace.package]
edition = "2021"
license = "MIT"
rust-version = "1.85"
repository = "https://github.com/NaniDerEchte2/Deadlock-Bots"

# Eine gepinnte Version pro Dependency — einzige Quelle der Wahrheit für den
# gesamten Workspace. Crates übernehmen via `<dep>.workspace = true`.
[workspace.dependencies]
dl-answer = { version = "0.1.0", path = "crates/dl-answer" }
# Async-Runtime
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "sync", "time", "process", "fs"] }
# Fehler-Typen (Domänen) / Fehler-Kontext (Binaries)
thiserror = "2"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
# SQLite — nur noch dl-central-etl liest Quell-SQLite-Snapshots.
rusqlite = { version = "0.32", features = ["bundled"] }
# Postgres — zentrale DB, Migrationen und compile-checked Queries.
sqlx = { version = "0.8", default-features = false, features = ["postgres", "runtime-tokio", "tls-rustls", "macros", "migrate", "chrono"] }
# HTTP-Server (dl-web; Broker folgt in Phase 2)
axum = "0.8"
tower-http = { version = "0.6", features = ["fs"] }
tower = "0.5"
# HTTP-Client (Dashboard-Relay, deadlock-api.com)
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "rustls-tls"] }
# Serde
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
regex = "1"
scraper = { version = "0.27.0", default-features = false }
# Zeit (ISO-Strings kompatibel zu Python datetime.isoformat)
chrono = "0.4"
chrono-tz = "0.10"
# Session-Signierung (HMAC-SHA256, Vertrag mit public_stats.py)
hmac = "0.12"
sha2 = "0.10"
base64 = "0.22"
hex = "0.4"
# Broker: dyn-fähige async Traits + Request-IDs
async-trait = "0.1"
# Discord (Gateway erst beim Cutover aktiv; REST sofort nutzbar)
serenity = { version = "0.12", default-features = false, features = ["client", "http", "gateway", "rustls_backend", "model", "cache", "utils", "voice"] }
songbird = { version = "0.6.0", default-features = false, features = ["driver", "gateway", "rustls", "serenity", "tungstenite", "receive"] }
hound = "3.5.1"
rand = "0.8"
# Namens-Normalisierung (streamer_link_matcher: NFKD-Deaccent)
unicode-normalization = "0.1"
# URL-Parsing (Dashboard-Redirect-Validierung, Sicherheits-Check)
url = "2"

# Tests
tempfile = "3"

# Interne Crates
dl-core = { version = "0.1.0", path = "crates/dl-core" }
dl-webcore = { version = "0.1.0", path = "crates/dl-webcore" }
dl-stats = { version = "0.1.0", path = "crates/dl-stats" }
dl-tierlist = { version = "0.1.0", path = "crates/dl-tierlist" }
dl-broker = { version = "0.1.0", path = "crates/dl-broker" }
dl-changelog = { version = "0.1.0", path = "crates/dl-changelog" }
dl-discord = { version = "0.1.0", path = "crates/dl-discord" }
dl-bridges = { version = "0.1.0", path = "crates/dl-bridges" }
dl-voice = { version = "0.1.0", path = "crates/dl-voice" }
dl-activity = { version = "0.1.0", path = "crates/dl-activity" }
dl-ai = { version = "0.1.0", path = "crates/dl-ai" }
dl-brain = { version = "0.1.0", path = "crates/dl-brain" }
dl-moderation = { version = "0.1.0", path = "crates/dl-moderation" }
dl-squads = { version = "0.1.0", path = "crates/dl-squads" }
dl-community = { version = "0.1.0", path = "crates/dl-community" }
dl-dashboard = { version = "0.1.0", path = "crates/dl-dashboard" }
dl-central-db = { version = "0.1.0", path = "crates/dl-central-db" }
dl-central-etl = { version = "0.1.0", path = "crates/dl-central-etl" }
dl-server-as-code = { version = "0.1.0", path = "crates/dl-server-as-code" }

[workspace.lints.clippy]
unwrap_used = "warn"
