{
  "db_name": "PostgreSQL",
  "query": "\n        WITH source AS (\n            SELECT started_at AT TIME ZONE 'UTC' AS utc_started,\n                   message_count,\n                   points\n            FROM activity.text_conversation_log\n            WHERE started_at >= $2\n              AND user_id = $3\n        ),\n        grouped AS (\n            SELECT CASE\n                    WHEN $1 = 'hour' THEN to_char(utc_started, 'HH24')\n                    WHEN $1 = 'day' THEN EXTRACT(DOW FROM utc_started)::INT::TEXT\n                    WHEN $1 = 'week' THEN to_char(utc_started, 'YYYY-') ||\n                        lpad(((EXTRACT(DOY FROM utc_started)::INT - 1 -\n                            ((8 - EXTRACT(ISODOW FROM date_trunc('year', utc_started))::INT) % 7) + 7) / 7)::TEXT, 2, '0')\n                    ELSE to_char(utc_started, 'YYYY-MM')\n                   END AS bucket,\n                   message_count,\n                   points\n            FROM source\n        )\n        SELECT bucket AS \"bucket!\",\n               COALESCE(SUM(message_count), 0)::BIGINT AS \"messages!\",\n               COALESCE(SUM(points), 0)::BIGINT AS \"points!\",\n               COUNT(*) AS \"sessions!\"\n        FROM grouped\n        GROUP BY bucket\n        ORDER BY bucket\n        ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "bucket!",
        "type_info": "Text"
      },
      {
        "ordinal": 1,
        "name": "messages!",
        "type_info": "Int8"
      },
      {
        "ordinal": 2,
        "name": "points!",
        "type_info": "Int8"
      },
      {
        "ordinal": 3,
        "name": "sessions!",
        "type_info": "Int8"
      }
    ],
    "parameters": {
      "Left": [
        "Text",
        "Timestamptz",
        "Int8"
      ]
    },
    "nullable": [
      null,
      null,
      null,
      null
    ]
  },
  "hash": "dbb5bf1d9c03c36f042f2f3bf609856dbedd8fd00582d5c8331c4ea6534a02f6"
}
